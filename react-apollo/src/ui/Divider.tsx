import * as React from "react";
import "./Divider.scss";

export const UIDivider: React.FC = () => (
    <div className={"ui-divider"}/>
);

export const UISeparator: React.FC = () => (
    <div className={"ui-separator"}/>
);
